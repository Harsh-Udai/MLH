# This is the simple programe to generate the random number.
# Random Library is used
import random

# In random library, we have randint function,
# in which we have 2 parameters.
# therefore it will give any no between these 2.

number = random.randint(1,1000)

# Printing the number
print("Random Number ->",number)